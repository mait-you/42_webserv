server {
    listen localhost:8080;
    server_name localhost;

    root ./www/webserver-tester;
    index index.html;

    client_max_body_size 1000000;

    location / {
        allow_methods GET POST DELETE;
        autoindex off;
    }
}
