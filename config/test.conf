events {}

http {
    error_log  /dev/stdout info;
    access_log /dev/stdout;

    server {
    listen 8081;
    server_name localhost;

    root /var/www/html;
    index index.html;

    location / {
        try_files $uri $uri/ =404;
    }
}
}
